{% extends 'base.html.twig' %}

{% block title %}Association{% endblock %}

{% block body %}
    <h1>Association</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ association.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('association_index') }}">Liste</a>

    <a href="{{ path('association_edit', {'id': association.id}) }}">editer</a>

    {{ include('association/_delete_form.html.twig') }}
{% endblock %}
